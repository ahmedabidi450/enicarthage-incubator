server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/enicarthage_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 'Khalil1939=$'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: mail@gmail.com
    password: pass
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

app:
  jwt:
    secret: enicarthage_incubator_jwt_secret_key_must_be_at_least_256_bits_long_for_security
    expiration: 86400000
    refresh-expiration: 604800000

  upload:
    dir: uploads/

logging:
  level:
    com.enicarthage: DEBUG
    org.springframework.security: DEBUG
